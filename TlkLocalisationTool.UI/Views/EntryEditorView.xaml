<views:ViewBase x:Class="TlkLocalisationTool.UI.Views.EntryEditorView"
                xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                xmlns:controls="clr-namespace:TlkLocalisationTool.UI.Controls"
                xmlns:views="clr-namespace:TlkLocalisationTool.UI.Views"
                mc:Ignorable="d"
                Title="{Binding Title}"
                WindowHeight="450" WindowWidth="800"
                d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Label Grid.Row="0" Content="Original"/>
        <TextBox Grid.Row="1" Text="{Binding OriginalValue}" IsReadOnly="True"/>
        <Label Grid.Row="2" Content="Localised"/>
        <controls:SpellCheckTextBox Grid.Row="3" Text="{Binding LocalisedValue}" Language="{Binding Language}" SpellCheckDictionaries="{Binding SpellCheckFileUris}"/>
        <Button Grid.Row="4" Content="Save" Command="{Binding SaveCommand}"/>
    </Grid>
</views:ViewBase>
